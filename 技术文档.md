# OmniDev 技术架构与核心逻辑文档

> 本文档基于源码梳理，介绍 OmniDev 开发环境控制台的整体技术选型、模块划分，以及其中若干**值得关注的特别逻辑**。
> 适用读者：接手维护本项目的开发者、需要理解其设计取舍的同学。

---

## 一、项目定位

**OmniDev** 是一个**纯本地运行**的多环境开发控制台（"开发大盘"）。它把日常前端开发里散落的操作收敛进一个图形界面：

- 多项目登记、激活切换
- 多环境（work/test/...）一键启停本地开发服务
- 进程级凭证注入 + 浏览器免密自动登录
- 远程测试服务器 SSH 分支切换、命令下发
- 本地/远程日志分流落盘与实时回显

核心设计原则贯穿全代码：**物理只读**（绝不修改业务项目里的任何文件）、**物理隔离**（每个项目配置/状态互不干扰）、**本机闭环**（服务只绑定 `127.0.0.1`）。

---

## 二、技术栈总览

- **前端** — **Vue 3** (`<script setup>` 组合式 API) + **Vite 4**。单页控制台，组件化拆分于 [src/views/console/](src/views/console/)
- **后端** — **Node.js + Express 5** (ESM `type: module`)。模块化路由 + 服务分层，入口 [server.js](server.js)
- **远程** — **ssh2**。SSH 长连接池、远程命令执行、Git 分支管理
- **桌面外壳** — **Tauri 2**（Rust）。可选打包为桌面应用，含系统托盘，见 [src-tauri/](src-tauri/)
- **自动登录** — **Python + Playwright**。由后端按需拉起的浏览器自动填表脚本 [scripts/auto_login.py](scripts/auto_login.py)
- **加密** — **Node crypto**（AES-256-CBC）。本地静默凭证保险库 [securityService.js](server/services/securityService.js)

### 进程拓扑

```
┌─────────────────────────────────────────────────────────┐
│  Tauri 桌面外壳 (可选, Rust/WebView) ── 系统托盘 + 窗口    │
│      │ 加载                                               │
│      ▼                                                    │
│  Vite 前端 (:3000)  ──/api 代理──▶  Express 后端 (:3300)  │
│                                          │                │
│                          ┌───────────────┼───────────┐    │
│                          ▼               ▼           ▼    │
│                   spawn 本地 dev      ssh2 远程    spawn   │
│                   服务 (:8080+)       服务器      python   │
│                                                  自动登录  │
└─────────────────────────────────────────────────────────┘
```

前端通过 Vite `server.proxy` 把 `/api` 转发到后端端口；端口值由 [vite.config.js](vite.config.js) 在启动时读取 `config/app.json` 动态决定，前后端端口配置**单一来源**。

---

## 三、后端模块划分

后端采用清晰的「路由（routes）— 服务（services）— 配置（config）」三层结构：

### 路由层 [server/routes/](server/routes/)
- **[projectRoutes.js](server/routes/projectRoutes.js)** — 项目登记/编辑/删除/激活、应用配置读写
- **[envRoutes.js](server/routes/envRoutes.js)** — 环境列表聚合、增删改、调起自动登录
- **[startRoutes.js](server/routes/startRoutes.js)** — 一键启动/停止本地开发服务（跨平台拉起）
- **[sshRoutes.js](server/routes/sshRoutes.js)** — 远程分支查询/检出、SSH 测试/断开/配置
- **[terminalRoutes.js](server/routes/terminalRoutes.js)** — 本地/远程命令下发、日志读取与清空
- **[configRoutes.js](server/routes/configRoutes.js)** — 团队配置脱敏导入/导出

### 服务层 [server/services/](server/services/)
- **[projectService.js](server/services/projectService.js)** — `projects.json` 管理、`appConfig` 动态代理
- **[envService.js](server/services/envService.js)** — 环境配置 CRUD、凭证加解密保险库、字段标准化
- **[processService.js](server/services/processService.js)** — 端口探测/分配、进程强杀、运行态托管
- **[sshService.js](server/services/sshService.js)** — SSH 连接池、闲置超时、远程命令
- **[securityService.js](server/services/securityService.js)** — AES 凭证保险库
- **[logService.js](server/services/logService.js)** — 分环境按天滚动日志、防抖合并落盘
- **[stateService.js](server/services/stateService.js)** — 运行态 `state.json` 持久化

### 配置层 [server/config/](server/config/)
- **[pathConfig.js](server/config/pathConfig.js)** — 按激活项目动态计算隔离路径 + 冷启动数据迁移

---

## 四、核心数据模型

配置全部存于本地 `config/` 目录（由 `config.example/` 经 `setup.ps1` 初始化生成，已被 `.gitignore` 排除）：

```
config/
├── app.json              # 全局应用配置（端口、项目级 preset 凭证注入预设）
├── projects.json         # 已登记项目清单 + activeProjectId
├── ssh.json              # SSH 配置（全局兜底，项目目录下的同名文件优先）
├── vault.json            # AES 加密后的凭证密文保险库
└── projects/
    └── <projectId>/      # 每个项目一套独立配置（物理隔离）
        ├── state.json            # 该项目的活跃环境 + 端口映射
        └── envs_common.json      # 该项目的公共环境定义（凭证字段内联，脱敏存储）

~/.omnidev_secret         # AES 主密钥（存于用户家目录，与项目物理分离, mode 0o600）
```

**环境标识**：环境键即 `envName`（如 `weike`），不再有子项目概念。凭证字段作为 `credentials` 数组内联在每个环境中。AES 保险库密钥按 `项目ID#环境名#字段名` 命名空间隔离。

---

## 五、特别逻辑详解（重点）

这一节是本项目真正"有意思"的地方，多数是为解决本地多环境开发的真实痛点而设计的非显然逻辑。

### 5.1 基于激活项目的物理隔离 + 冷启动无损迁移
见 [pathConfig.js](server/config/pathConfig.js)。

- 项目级配置（`state` / `envs_common`）路径都按当前 `activeProjectId` **动态计算**，切项目即切整套配置，互不污染。
- 首次为某项目生成隔离目录时，会判断是否是**第一次迁移**（`projects/` 下还没有别的项目目录）：
  - 是 → 把老的全局 `config/*.json` **复制**进项目目录（老用户无损升级）；
  - 否 → 从 `config.example/` 模板初始化（新项目干净起步）。
- SSH 配置也按项目隔离：`config/projects/<id>/ssh.json`。新项目首次初始化时从全局 `config/ssh.json` 或 `config.example/ssh.json` 模板拷贝。

### 5.2 `appConfig` 的 ES6 Proxy 热插拔
见 [projectService.js](server/services/projectService.js#L68)。

`appConfig` 不是普通对象，而是一个 `Proxy`：每次属性访问都实时调用 `getActiveAppConfig()`，把全局 `app.json` 与**激活项目根目录下的 `.omnidev.json`** 合并返回。于是：

- 业务代码到处写 `appConfig.primarySubproject` 即可，无需关心当前是哪个项目；
- 每个业务仓库可以放一份 `.omnidev.json` 自描述其注入预设，切到该项目即自动生效；
- 切项目时调用 `clearAppConfigCache()` 失效缓存即可热重载。

### 5.3 凭证安全保险库（静默 AES，规避钥匙串弹窗）
见 [securityService.js](server/services/securityService.js) + [envService.js](server/services/envService.js)。

- 用 **AES-256-CBC** 自管加密，而非系统原生 Keychain/DPAPI——目的是**避免读取系统钥匙链时弹出的授权窗口**，换取零打扰体验。
- 主密钥 32 字节随机生成，存于 `~/.omnidev_secret`（`mode 0o600`），与项目配置物理分离。
- 密文格式 `iv_hex:encrypted_hex`，每条记录独立 IV。
- **写时脱敏**：保存环境配置时，密码与凭证明文被剥离，物理 JSON 文件里 `value` 强制清空，密文仅进 `vault.json`。
- **读时回落**：读取时优先从保险库实时解密，无记录则回落读物理明文，保证老数据"防丢"。
- 返回前端的接口数据再经 `sanitizeEnvData` / `sanitizeSSHConfig` **二次脱敏**（黑名单字段直接删除、密码显示为 `******`）。

### 5.4 多策略端口探测与"影子端口"自愈
见 [processService.js](server/services/processService.js) + [envRoutes.js](server/routes/envRoutes.js#L236)。

本地多环境并行最大的坑是"端口状态脏数据"，这里有三套机制：

1. **三种探测手段各司其职**：
   - `isPortOccupied`：`net.Socket` 微探测 + **1800ms 内存缓存**，供高频轮询用，CPU≈0；
   - `isPortAvailableAsync`：真实 `createServer().listen()`，分配端口前做精准可用性判断；
   - `getPortPid`：`netstat`(win)/`lsof`(unix) 拿 PID，用于强杀。
2. **启动时端口排他**：分配到端口后，清除 `envPorts` 里其它环境残留的同端口记录。
3. **列表刷新时"影子端口"修剪**：`/api/envs` 聚合时，若多个环境都指向同一个已占用端口，按"内存活跃子进程 > 当前激活环境 > 列表末位"的优先级**选出唯一获胜者**，其余记录清除并持久化，治愈脏状态。

### 5.5 跨平台启动 + 自动 Node 版本切换（fnm）+ 进程级凭证注入
见 [startRoutes.js](server/routes/startRoutes.js)。

一键启动 `/api/start` 做了很多平台适配工作：

- **三平台分别拉起**：Windows 弹独立 PowerShell 窗口、macOS 用 AppleScript 拉 Terminal、Linux/WSL 后台子进程；输出统一重定向落盘。
- **自动探测 Node 版本**：读取项目工作目录 `.nvmrc`（并处理 **UTF-16 LE/BE/UTF-8 BOM** 字节序与乱码，正则抽纯版本号），或 `package.json` engines；在启动命令前插入 `fnm use <ver> --log-level quiet` 自适应切换。
- **智能匹配启动脚本** `resolveNpmRunCommand`：按 `app.json` 的 `npmScriptMap` → `dev:<env>` → `<env>` → `dev/serve/start` 顺序在 `package.json` 里挑脚本。
- **进程级临时环境变量注入**：按 `appConfig.preset.envVarMap` 规则（支持 `ENV_KEY` 或 `ENV_KEY:template={{VALUE}}` 模板），把凭证编译成 `process.env` 注入子进程。**严格物理只读**——绝不在业务项目里写 `.env` 等任何文件。

### 5.6 SSH 长连接池：复用 + 闲置超时 + 心跳 + 死链自愈
见 [sshService.js](server/services/sshService.js)。

- 以 `host:port:user:pass` 为 key 缓存单条长连接，配置一致则复用并刷新计时器。
- **5 分钟闲置自动断开**；每次使用（连接、命令结束）都重置定时器。
- 连接参数带 **10s keepalive 心跳**（`keepaliveInterval`），防中继断链。
- `exec` 报错时主动 `end()` 并清空缓存，**防止下次复用到已单向死掉的 TCP 连接**。
- 进程退出时由 [server.js](server.js) 的优雅退出钩子统一 `disconnectSSH()`。

### 5.7 远程分支"强力干净切换"
见 [sshRoutes.js](server/routes/sshRoutes.js#L78)。一条命令链完成重置+拉取+切换：

```bash
cd <远程路径> && git checkout . && git clean -df && git pull && git checkout <branch>
```
远程绝对路径由 `getRemoteEnvPath` 解析（支持绝对路径或相对 `ssh.remote_path` 父目录拼接）。

### 5.8 日志系统：防抖合并 + 增量上下文前缀 + 尾部读取 + GBK 自愈
见 [logService.js](server/services/logService.js) + [terminalRoutes.js](server/routes/terminalRoutes.js)。

- **100ms 防抖合并落盘**：同一时间片的日志流汇聚成一次 `appendFile`，平滑 I/O。
- **增量上下文前缀**：3 秒内同环境、同分类、同文件的连续输出**不重复打印大标题**，只缩进追加——专门消除网络 Buffer 分片导致的标题穿插与断字。
- **尾部读取** `readTailOfFileAsync`：日志接口只读文件末尾 100KB，避免大文件同步阻塞。
- **GBK 乱码自愈** `decodeBuffer`：Windows 下先按 UTF-8 解，若出现 Unicode 替换字符（U+FFFD）再用 `TextDecoder('gbk')` 兜底。
- **Git 提示分流**：把 `From `/`Fast-forward`/`->` 等 Git 正常诊断信息从"致命错误"中分流出来，避免前端误报红框。
- 本地命令优先用系统 PATH 中定位到的 **Git Bash** 执行（`grep/awk/sed` 原生可用），找不到再降级 PowerShell。
- 日志按 `local_<env>_<date>.log` / `remote_..` 分流，自动滚动**只保留最近 3 天**。

### 5.9 浏览器免密自动登录（Python Playwright，自愈式）
见 [scripts/auto_login.py](scripts/auto_login.py)，由 [envRoutes.js](server/routes/envRoutes.js#L548) 通过 `spawn('python', ...)` 拉起，stdout 实时回流进前端终端面板。

- **依赖自愈**：缺 `playwright` 自动 `pip install`（清华源加速）；缺 Chromium 内核自动 `playwright install chromium`。
- **浏览器通道降级**：按用户偏好 `chrome → msedge → 内置 Chromium` 依次尝试拉起。
- **启发式表单识别**：先找 `type=password`，再按 name/id/placeholder 关键字找账号框，多重兜底；登录按钮按"登/login/确定/提交"文案匹配，找不到就在密码框按回车。
- 登录后浏览器**无限期保持**，直到用户手动关闭所有标签页脚本才退出。
- 同源凭证另有前端注入通道 [loginAdapters.js](src/utils/loginAdapters.js)：把凭证按 `inject_type` 写入 Cookie / localStorage / sessionStorage 后跳转直达链接（`{{VUE_DEV_HOST}}` 模板替换）。

### 5.10 全局跨项目进程管理与优雅退出
见 [processService.js](server/services/processService.js#L233) + [server.js](server.js)。

- `killActiveProcess()` 不只杀内存里托管的子进程，还会**遍历所有已登记项目的 `state.json`**，按端口反查 PID 全局强杀（`taskkill /F /T` 含子进程树），并清空各项目端口映射——与前端"运行中服务"面板逻辑完全对齐，不遗漏不误杀。
- **自毁接口** `/api/system/shutdown`：前端关闭应用时调用，可选 `killEnvs` 决定是否连带终结本地开发环境；用 `pendingKillEnvs` 标志位防止信号处理器竞态覆盖（`SIGINT` 会传字符串 `'SIGINT'`，必须严格过滤非布尔值）。

### 5.11 团队配置安全导出/导入
见 [configRoutes.js](server/routes/configRoutes.js)。导出当前公共环境**凭证字段架构**，但 **100% 剥离** 密码/Cookies/Token 的具体 `value`，只保留结构与占位——团队成员导入后填自己的凭证即可，敏感信息绝不外泄。

### 5.12 Tauri 桌面外壳：托盘 + 拦截关闭
见 [src-tauri/src/lib.rs](src-tauri/src/lib.rs)。

- 构建系统托盘（"显示主窗口"/"退出"），左键点击托盘恢复窗口。
- 监听 `CloseRequested` 时 `prevent_close()` **阻止默认销毁**，转而 `emit('tauri-close-requested')` 交给前端弹出关闭偏好确认（后台保留 / 连带杀环境），实现"点 X 不是直接退出"的体验。

### 5.13 打包运行态配置隔离自愈、运行日志重定向与 API 劫持
为了解决打包发布后 Windows 环境下默认安装在只读目录（如 `C:\Program Files`）导致的配置写入拒绝（EACCES）及运行调试盲区，引入了自适应 AppData 迁移和日志物理重定向：

- **AppData 物理配置隔离与增量自愈**：
  - 在生产打包运行环境中，后端读取和保存配置的路径会自适应重定向至用户专属的 AppData 目录：`C:\Users\<用户名>\AppData\Roaming\com.lin.omnidev\config`（见 [pathConfig.js](server/config/pathConfig.js)）。
  - 如果该目录首次运行为空，系统会自动通过 `copyConfigFolderRecursive` 将安装包资源中自带的默认配置模版、加密凭证 `vault.json` 以及项目列表 `projects/` 递归增量复制过去，保证原有设置与用户凭证完美无损带入。
- **后台 Node.js 运行日志物理重定向**：
  - Rust 层在双击拉起后台 Node 进程时，自动将其 `Stdout` 和 `Stderr` 重定向到 `%APPDATA%\com.lin.omnidev\logs\node_server.log`（见 [lib.rs](src-tauri/src/lib.rs)），摆脱了生产环境无控制台窗口时 Node 后端“无声闪退”的调试盲区，实现任意运行期报错的可追溯性。
- **后端端口 API 动态拦截代理**：
  - 前端在 [main.js](src/main.js) 中加入全局拦截器，通过 Rust 定义 of `get_backend_port` 命令动态请求后端当前运行的实际端口，并将所有以 `/api/` 开头的 fetch 请求重定向到本端 Node 服务端口（`http://localhost:<端口>`），解决打包后静态资源直接运行产生相对路径 404 的问题，且天然兼容开发态下的 Vite 代理。
- **一键打开运行日志目录**：
  - 系统设置的“关于与更新”面板中加入了“📂 打开运行日志目录”按钮，前端点击后请求后台 `/api/system/open-log-dir` 接口，后端自动识别当前环境并唤起系统文件管理器定位至日志目录。

---

## 六、四大安全防线（README 对应的代码落点）

- **物理闭环隔离（仅绑 `127.0.0.1`）** — 各 `net` 探测与启动均锁定本机环回口
- **路径穿越根治** — 环境标识经 `replace(/[^a-z0-9_-]/g, '')` 强清洗后才参与文件读写
- **并发竞态防刷** — 前端 SSH 操作 UI 禁用锁 + 后端连接池死链清理 + `pendingKillEnvs` 防竞态
- **日志 XSS 免疫** — 前端终端回显全程 Vue 安全插值，禁用 `v-html`

---

## 七、快速上手

一条命令一键开箱（`setup.ps1` 会完成：复制配置 → `npm install` → 自动登录依赖 → 询问是否启动）：

```powershell
.\setup.ps1                 # 交互式一键初始化，结尾询问是否启动
```

无人值守 / 进阶参数：

```powershell
.\setup.ps1 -Yes -Run       # 全自动：装好一切并直接启动
.\setup.ps1 -SkipInstall    # 跳过 npm install
.\setup.ps1 -SkipAutoLogin  # 跳过 Python/Playwright 自动登录依赖
```

> `setup.ps1` 存为 **UTF-8 with BOM**，以保证 Windows PowerShell 5.1 在简体中文系统下按 UTF-8 而非 GBK 解析，避免中文乱码导致的伪语法错误。
> 首次运行 `.ps1` 若被执行策略拦截：`Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`。
> 也可手动 `npm install` 后 `npm run dev`。

- **`config/app.json`** — 端口、项目级 preset 凭证注入预设
- **`config/projects.json`** — 本地项目真实物理绝对路径 + 激活项
- **`config/projects/<id>/ssh.json`** — 该项目专属的远程 SSH 凭证
- **`config/projects/<id>/`** — 每个项目独立的 state / envs_common
- **`<项目根>/.omnidev.json`** — 项目级自描述配置（覆盖 app.json）

---

## 八、设计取舍小结

- **自管 AES 而非系统钥匙串** — 规避授权弹窗，零打扰
- **物理只读 / 进程级环境变量注入** — 绝不污染业务项目工作区
- **按项目隔离配置 + Proxy 热重载** — 多项目互不干扰，切换零成本
- **Socket 探测 + 内存缓存** — 高频轮询下 CPU 近 0
- **影子端口排他修剪** — 根治多环境并行的端口脏状态
- **日志防抖 + 增量前缀** — 解决 Buffer 分片导致的日志错乱
- **SSH 长连接池 + 心跳 + 死链自愈** — 远程操作稳定不卡顿

---

*本文档由源码梳理自动生成，若代码演进请同步更新。*
